# Server-only Gemini configuration. Add the real value in Vercel Environment Variables,
# never in frontend code, GitHub, or chat.
GEMINI_API_KEY=
# Optional stable model override. Default: gemini-2.5-flash
GEMINI_MODEL=gemini-2.5-flash
